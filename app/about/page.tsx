import type { Metadata } from "next";
import Link from "next/link";
export const metadata: Metadata = { title: "About XXF Tools", description: "Why XXF builds fast, private JSON and frontend conversion tools for everyday developer work.", alternates: { canonical: "/about/" } };
export default function AboutPage() { return <main className="legal-page"><div className="legal-page__inner shell"><span className="kicker">About XXF</span><h1>Useful conversions, explained</h1><p>XXF Tools is a focused collection of browser-based utilities for the small transformations that interrupt frontend work: formatting JSON, generating types, translating data formats, decoding text and preparing media.</p><h2>Private by architecture</h2><p>Text, image and video transformations run on your device. Converter input is not uploaded to an XXF processing API, stored in an account or reviewed by us. The redirect checker sends only the public URL being tested to its restricted server endpoint, while the M3U8 player connects directly to the stream host.</p><h2>Editorial approach</h2><p>Every tool page documents the expected input, a repeatable workflow, useful scenarios and limitations specific to that format. The guidance is reviewed against the implementation so it explains what the tool actually does rather than repeating generic definitions.</p><h2>Review before production</h2><p>Generated code, schemas and converted data are starting points. Samples cannot reveal every optional field, domain rule, parser difference or security requirement, so XXF keeps source and output visible and calls out decisions that require human review.</p><h2>Independent and inspectable</h2><p>XXF is free to use without registration. Advertising may support hosting and maintenance, but tool access is not gated behind an account. The implementation is available in the <a className="text-link" href="https://github.com/sufaith/xxf-json-frontend-toolbox" target="_blank" rel="noreferrer">public source repository</a>, and feedback can be submitted through the <Link className="text-link" href="/contact/">contact page</Link>.</p></div></main>; }
